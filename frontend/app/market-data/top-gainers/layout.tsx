import { pageMetadata } from "@/lib/seo-meta";

export const metadata = pageMetadata("/market-data/top-gainers");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
