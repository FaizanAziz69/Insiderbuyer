import { pageMetadata } from "@/lib/seo-meta";

export const metadata = pageMetadata("/market-data/top-losers");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
