import { pageMetadata } from "@/lib/seo-meta";

export const metadata = pageMetadata("/learn/insider-buying");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
