import { pageMetadata } from "@/lib/seo-meta";

export const metadata = pageMetadata("/insiders/buy-sell");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
